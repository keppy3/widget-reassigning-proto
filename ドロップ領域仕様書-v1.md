# ドロップ領域とは

現在ドラッグ中のウィジェットをどこに置くかを示すインジケーターとその領域のこと。ドロップすると実際にその位置にウィジェットが配置される。
他ウィジェットとの兼ね合いで配置できない場所などがあり、実際にドラッグしている位置とドロップ領域はずれる事がある。

# ドロップ領域決定ロジック

ドロップ領域は以下の3段階で決定する

1. カーソル位置による判断
2. 環境条件による判断
3. 直感補正

## ①カーソル位置による判断

ドラッグ中カーソルがどこにあるかだけを変数に仮のドロップ領域を決定する。どの並び替えロジックでも共通。

- XS: カーソル座標があるセル（カーソルが領域外の場合、最も近いセル）
- S: カーソル座標がある列（カーソルが領域外の場合、最も近い列）
- L: ページとページの間、およびページの中線を境界に判断。カーソルが任意のページの左半分にあるなら、そのページの1-2列目、右半分にあるならそのページの2-3列目

## ②環境条件による判断

他ウィジェットの配置状況などの制約で①の場所に配置できない場合、並び替えロジックによる左詰め処理等により、仮のドロップ領域を変更

## ③直感補正

①と②のズレが直感と合わない場合、ドロップ領域の更新を保留し、直前の位置に留める。保留は①②のズレが解消される座標に移動するまで継続する。

「直前の位置」とは、直近で③もクリアして実際に更新されたドロップ領域のこと。ドラッグ開始直後でまだ直前の位置が無い場合は、ドラッグ中ウィジェットの元の位置を指す。

直感にそぐわないズレは以下の通り

- XS/Sをドラッグしているとき、①と②の列が1列以上ズレている（ページを跨いでも列のズレとして連続的に数える）
- Lをドラッグしているとき、①と②が異なるページにある
- ただし、②③（重力のように常時左詰めへ動き続けるロジック）において、①が現在配置されている他ウィジェットの一番右の列より先にある場合は例外（②に吸い寄せられても直感に反しない）
