# 前提情報

## 共通仕様

- ウィジェット配置可能領域は複数のページで構成される
- ページ内は3列×2行のセルで構成される
- ウィジェットサイズバリエーションと占有セル数
  - XS (1×1 = 1セル占有)
  - S (1×2 = 2セル占有)
  - L (2×2 = 4セル占有)

- ウィジェットは他のウィジェットと重なってはならない
- ウィジェットはページを移動できるが、部分的にはみ出してはならない
- ドラッグ中他のウィジェットがどう動くか常にプレビューを表示し、ドロップ領域が変わるたびに再計算する
- ウィジェットの配列が変わるたびに、常に一番右に空白のページが1つある状態にする（動的に追加・削除を行う）
- ドラッグ中に辿った経路は考慮せず、起点とドロップ領域のみを見て再配置を行う。
- 他のウィジェットがドラッグ中ウィジェットの配置を妨げることはない。ドロップ領域にいる他のウィジェットは必ずどかされ、空きスペースが作られる
- Lを配置する際に右半分がページに収まらない場合、次ページに繰り上げて配置。元のページの余った列は空欄のまま残る

> [!TIP]
> ### 起点：
> 今回のドラッグでウィジェットを掴んだ時点の、元のページ・列。ドラッグ中に辿った経路とは無関係の固定値。
> 例：列1にあるウィジェットをドラッグし、列2にホバーしている場合、起点は列2から見て「左」

> [!TIP]
>
> ### ドロップ領域：
> ドラッグ中ウィジェットが今まさにホバーしている領域。ここに配置されることを明示するため、配置可能な場合はAccent色で、不可能な場合はDanger色でハイライトされる。配置条件を満たす場合、ドロップするとその領域にウィジェットが配置される。

## セルの構成とインデックス

1ページ内は3列2行の6セルで構成され、以下のようにインデックスを割り振るものとする。
「次のセル」や「次の列」などと言うときはこのインデックスを参照する。

|         | 列1  | 列2  | 列3  |
| ------- | ---- | ---- | ---- |
| **行1** | 1    | 3    | 5    |
| **行2** | 2    | 4    | 6    |


# ①シフト自由配置 の再配置ロジック

## 概要

- iOSのような自動左詰めは行わず、歯抜けを許容する
- ドロップ領域にウィジェットがある場合、まず上下左右に空きスペースがないか探索し、あれば移動する。移動できなかったウィジェットはドロップ領域の次の列へ移動する。移動先にウィジェットがある場合それも右へ移動し、移動が発生しなくなるまでこれを繰り返す

## フローチャート


```flow
start=>start: ドロップ領域が更新された
end=>end: END
cond1=>condition: ドラッグ中ウィジェットの
ドロップ領域が空いている
sub1=>subroutine: 空きセル探索


cond2=>condition: 空きセル探索により
ドロップ領域が空いた
op2=>operation: 処理しきれなかったウィジェットを
シフト・キューに追加
sub2=>subroutine: 連鎖シフト処理

cond3=>condition: シフト・キューが空である

start->cond1(no,bottom)->sub1->cond2(no,bottom)->op2->sub2->cond3(no,left)->sub2
cond1(yes,right)->end
cond2(yes,right)->end
cond3(yes,right)->end

```

## 空きセル探索

ドロップ領域内を占有している邪魔なウィジェット全てを対象に、左右上下の隣接するセルを空きがないか探し、ある場合そこへ移動する処理。

- 空きセル探索で逃げ場のなかったウィジェットはシフト・キューへ送られる
- 縦並びのXSはユニット化したままキューに入る

###### 表1. ドラッグ中ウィジェットと探索対象ウィジェットによる処理の違い

|                        | L / S      | 上/下が空きセルのXS          | 縦並びの2つのXS                       |
| ---------------------- | ---------- | ---------------------------- | ------------------------------------- |
| **L / S をドラッグ中** | 左右を探索 | ①上下を探索<br />②左右を探索 | ①2つのXSをユニット化<br />②左右を探索 |
| **XS をドラッグ中**    | 左右を探索 | ①上下を探索<br />②左右を探索 | ①上下を探索<br />②左右を探索          |

> [!NOTE]
> 空きセル探索では、できるだけシフト処理を発生させないように占有ウィジェットを四方へどかす試みを行う。ドラッグ中のウィジェットがLかSの場合、縦並びの2つのXSのうち1つが避けれたとしてももう1つが避けれなければシフトは発生してしまうことに変わりはないので、これらはむしろバラけないためにユニットとして扱う

> [!TIP]
> ### シフト・キュー：
> 空きセル探索で逃げ切れなかったウィジェットを連鎖シフト処理で再配置するためのキュー。
> シフト処理で再配置する際に並び順を整えたり、再配置先にあるウィジェットを動的に回収するために使う。

## 連鎖シフト処理

邪魔なウィジェットをドロップ領域の右側へずらしていく処理。

- シフト・キューに入っているウィジェットをドロップ領域の次の列から順に配置
- 配置先に既にウィジェットがある場合、そのウィジェットをキューへ移動する

# ②シフト左詰め の再配置ロジック

## 概要

- ウィジェット単位ではなくユニット単位で "順番" を管理し、その順番通りに左詰めで敷き詰める
- ユニットの定義は次の通り
  - Sユニット：Sウィジェット1つ（1列分占有）
  - Lユニット：Lウィジェット1つ（2列分占有）
  - XS+XSユニット：縦に並ぶXSウィジェット2つで1ユニットとして扱う（1列分占有）
  - XS+空きユニット：縦に並ぶXSウィジェットと空きセルで1ユニットとして扱う（1列分占有）
- ドロップ領域が変わるたび、ドラッグ中ユニットの順番が更新され、再配置される。
  ドロップ領域より左側にあるユニットの総数がN個とし、ドラッグ中ウィジェットの順番は#N+1である
- 例外として、ドラッグ中ウィジェットがXSであり、XS+空きユニットと重なっている場合、ユニットの順番を変えずにそのユニット内へ収める

## フローチャート
```flow
st=>start: ドロップ領域が設定される
end=>end: END

cond1=>condition: ドラッグ中ウィジェットが
XSである
cond2=>condition: ドラッグ中ウィジェットが
Sである
cond3=>condition: ドロップ領域とXS+Oユニットが
重なっている
cond4=>condition: ドロップ領域とXSウィジェットが
重なっている

op_l=>operation: ドラッグ中ウィジェットを
Lユニット扱いとする
op_s=>operation: ドラッグ中ウィジェットを
Sユニット扱いとする
op_xso=>operation: ドラッグ中ウィジェットを
XS+空きユニット扱いとする
op_nudge=>operation: そのXSを上/下の
空きセルへどかす
op_xsxs=>operation: ドラッグ中のXSをXS+空きユニットに含めて
XS+XSユニット扱いとする

op_order=>operation: ドラッグ中ウィジェットの順番を更新
op_relayout=>operation: 新しい順番に従って
左/上詰めで再配置

st->cond1(no,bottom)->cond2(no,bottom)->op_l->op_order->op_relayout->end
cond2(yes,right)->op_s->op_order
cond1(yes,right)->cond3(no,bottom)->op_xso->op_order
cond3(yes,right)->cond4(no,bottom)->op_xsxs->op_order
cond4(yes,right)->op_nudge->op_xsxs

```


# ③シフト 左・上詰め の再配置ロジック

## 概要

- 各ウィジェットの "順番" を保持し、その順番通りに左詰め・上詰めで敷き詰める
  - 例えば S, XS, S と並んでいる場合、XSは上詰めされ、下には空きセルができる
- ドロップ領域が変わるたび、ドラッグ中ウィジェットの順番が更新され、再配置される。
  ドロップ領域より左側にあるウィジェットの総数がN個とし、ドラッグ中ウィジェットの順番は#N+1である

## フローチャート
```flow
start=>start: ドロップ領域が更新された
end=>end: END
op1=>operation: ドラッグ中ウィジェットの "順番" を更新
op2=>operation: 新しい "順番" に従って左/上詰めで再配置

start->op1->op2->end

```
